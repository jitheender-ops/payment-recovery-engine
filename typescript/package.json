{
  "name": "payment-recovery-engine-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript replication of the deterministic decision core of the Payment Failure Recovery Engine",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/*.test.js\"",
    "demo": "npm run build && node dist/demo.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
